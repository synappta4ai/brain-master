# Dependencias del Worker de Inferencia Brain-Master (imagen GPU)
# gRPC
grpcio==1.84.0
grpcio-tools==1.84.0
protobuf>=5.29.3

# IA core (torch se instala aparte con index CUDA 12.4 en el Dockerfile)
diffusers>=0.36.0
transformers>=4.57.0
tokenizers>=0.22.0
accelerate>=1.12.0
bitsandbytes>=0.45.0 ; platform_system == "Linux" and platform_machine == "x86_64"
safetensors
sentencepiece
einops
open_clip_torch

# Media (encoder MP4 del motor de video)
imageio>=2.37.0
imageio-ffmpeg>=0.6.0
Pillow

# Operación
psutil
numpy
