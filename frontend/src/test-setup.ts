/*
 * El builder @angular/build:unit-test inicializa TestBed automáticamente.
 * Este archivo existe como punto de extensión para polyfills/globales
 * futuros de testing (se referencia desde tsconfig.spec.json).
 */
export {};
